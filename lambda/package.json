{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Lambda code for",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cd ./dist && zip ../bundle.zip *",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris Burton",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.33"
  }
}
